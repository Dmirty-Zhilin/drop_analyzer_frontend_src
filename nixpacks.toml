[phases.setup]
nixPkgs = ["nodejs", "nodejs.pkg.npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production npm start"

[nixpacks]
framework = "nextjs"
